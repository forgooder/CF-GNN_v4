# 2026-06-03 18:37 CST Work Report

## Scope

Ran a validation-only `nell_v1` alpha-budget diagnostic. No test-set evaluation was run.

## Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d nell_v1 -e diag_v5_nell_v1_alpha_budget035_10ep \
  --use_causal_training \
  --num_epochs 10 \
  --batch_size 16 \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.0 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.1 \
  --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_target 0.35 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect
```

## Result

Best validation AUC/AUC-PR was 0.9054/0.9146.

Key mask checkpoints:

```text
epoch1 raw=0.3482/0.4440 entropy=0.4761/0.6863 budget=0.0665 logit_l2=5.6705
epoch5 raw=0.7914/0.4250 entropy=0.1229/0.6809 budget=0.3258 logit_l2=50.6074
epoch10 raw=0.8050/0.4239 entropy=0.1171/0.6809 budget=0.3300 overlap=0.3365 logit_l2=50.4665
```

## Conclusion

This is a negative diagnostic. The lower causal target and stronger budget term produce a healthy first epoch, but alpha still drifts open and low-entropy by epoch5 and remains unhealthy at epoch10. Validation AUC-PR also drops from the prior causal-only 10 epoch run's 0.9190 to 0.9146.

## Next

Do not run test evaluation. The next useful NELL_v1 step is an alpha-specific default-off control, such as separate alpha entropy floor/logit L2 weights, or a staged/frozen mask schedule.
