# 2026-06-03 11:46 CST Work Report

## Scope

Continued Causal-GraIL v5 from the completed WN18RR_v1 entropy-floor diagnostic. The next priority is WN18RR_v2, so this round ran a smoke check before committing to a full formal run.

## Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d WN18RR_v2 -e smoke_v5_wn18rr_v2_stronger_beta_budget \
  --use_causal_training \
  --num_epochs 1 \
  --batch_size 4 \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.1 \
  --effect_loss_warmup_epochs 10 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect
```

Commit: `e3dd850`

## Result

The WN18RR_v2 causal path ran successfully for 1 epoch and built the experiment LMDB caches under `experiments/`.

Best validation performance observed during the smoke epoch:

```text
AUC    0.9414502031232795
AUC-PR 0.9485761101267314
```

Final epoch training summary:

```text
training auc    0.8612964878634677
training auc_pr 0.8969800042133036
best validation AUC 0.9414502031232795
```

Mask health at epoch 1:

```text
causal_mask_raw_mean   0.7470803085364478
shortcut_mask_raw_mean 0.37900323619913756
causal_mask_entropy    0.17316450138760114
shortcut_mask_entropy  0.659800887857593
mask_budget_loss       0.22940247908292682
mask_overlap_loss      0.269547248695012
```

## Interpretation

The smoke passed: model construction, causal scoring, DGL path, validation, and checkpoint saving all work on WN18RR_v2.

The masks are not collapsed at epoch 1. Shortcut raw mean is safely above zero and shortcut entropy is high. Causal raw mean is already high at 0.7471, so the formal run needs close monitoring for alpha saturation and overlap growth.

`eval_every_iter=455` caused multiple validation checks inside the single smoke epoch. The ledger records the best validation AUC/AUC-PR for smoke diagnostics only; this is not treated as a formal result.

## Next Step

Run the formal WN18RR_v2 experiment using the same stronger-beta-budget configuration, then evaluate all score modes on test and run ranking on the best AUC-PR mode.
