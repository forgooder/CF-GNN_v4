# 2026-06-03 15:36 Work Report

## Scope

Moved from WN18RR_v2 scalar/schedule/clamp diagnostics to the next priority dataset, WN18RR_v4. Ran a one-epoch Causal-GraIL v5 smoke with stronger-beta-budget settings and no test-set evaluation.

## Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d WN18RR_v4 -e smoke_v5_wn18rr_v4_stronger_beta_budget \
  --use_causal_training \
  --num_epochs 1 \
  --batch_size 4 \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.1 \
  --effect_loss_warmup_epochs 10 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect
```

## Execution

The run built WN18RR_v4 train/valid LMDB caches under `experiments/`. Cache/checkpoint/log files were not committed.

## Validation

```text
best validation AUC 0.9225
best validation AUC-PR 0.9367
```

## Mask Diagnostics

```text
epoch1 effect_loss_weight=0.0
epoch1 raw causal=0.2078
epoch1 raw shortcut=0.4310
epoch1 entropy causal=0.0829
epoch1 entropy shortcut=0.6803
epoch1 budget_loss=0.1993
epoch1 overlap_loss=0.0761
```

## Conclusion

Smoke passed: WN18RR_v4 data path, causal training path, validation, and checkpoint save all work.

Mask health is mixed. Shortcut is near the 0.45 target and has high entropy. Causal raw is low and causal entropy is already near the unhealthy threshold, so alpha collapse should be monitored before any formal test-set run.

## Next Action

Run a short WN18RR_v4 validation/mask diagnostic using the now-built cache. Do not run test evaluation unless mask health is materially better than the WN18RR_v2 diagnostics.
