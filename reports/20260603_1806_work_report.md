# 2026-06-03 18:06 CST Work Report

## Scope

Ran and recorded the first next-priority `nell_v1` smoke after WN18RR_v4 diagnostics. This was validation and mask-health only; no test-set evaluation was run.

## Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d nell_v1 -e smoke_v5_nell_v1_logit_l2_causal_only \
  --use_causal_training \
  --num_epochs 1 \
  --batch_size 4 \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.0 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect
```

## Result

The run built `nell_v1` train/valid subgraph caches and completed 1 epoch.

Best validation AUC/AUC-PR was 0.8957/0.9018.

Epoch 1 mask stats:

```text
raw causal=0.4521
raw shortcut=0.4307
entropy causal=0.3080
entropy shortcut=0.6798
budget_loss=0.1506
overlap_loss=0.1842
mask_logit_l2_loss=17.8342
weight_norm=163.0245
```

## Conclusion

This is a positive smoke for the NELL_v1 path. Raw alpha/beta means are close to target and beta entropy is healthy. Alpha entropy is lower than ideal but there is no hard mask collapse in the first epoch.

## Next

Run a longer validation-only `nell_v1` diagnostic with the same logit-L2 causal-only setup before considering any test-set evaluation.
