# 2026-06-04 11:26 CST Work Report

## 1. Git State

Branch: `v5-alpha-beta-mask`

Current code base:

```text
07ff007 Add WN18RR v4 original score diagnostic report
08b3c9c Add WN18RR v2 original score diagnostic report
923d0bc Add WN18RR v1 original score formal test report
```

No code changes were made in this round.

## 2. Goal

Run the NELL_v1 original-score/all-mode validation diagnostic using the current causal-only alpha-specific regularization path. Because validation and masks were healthy enough, run one formal test/ranking evaluation.

## 3. Training / Validation Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d nell_v1 -e diag_v5_nell_v1_original_score_w05_allmodes_10ep \
  --gpu 0 \
  --use_causal_training \
  --num_epochs 10 \
  --batch_size 16 \
  --causal_loss_weight 0.5 \
  --effect_loss_weight 0.0 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_entropy_floor_weight 0.1 \
  --causal_mask_logit_l2_weight 0.002 \
  --mask_entropy_floor 0.2 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode original \
  --selection_metric auc_pr \
  --log_all_score_modes_validation
```

## 4. Validation Result

Best validation checkpoint by original AUC-PR:

```text
original AUC/AUC-PR 0.9073/0.9234
```

All-mode snapshot at the selected validation point:

```text
original AUC/AUC-PR 0.9073/0.9232
causal AUC/AUC-PR 0.9065/0.9219
shortcut AUC/AUC-PR 0.9045/0.9213
effect AUC/AUC-PR 0.7323/0.8211
causal_plus_effect AUC/AUC-PR 0.9059/0.9220
```

The validation result was close to the prior NELL_v1 alpha-specific validation high point and far above the same-environment NELL_v1 baseline AUC-PR 0.7967, so one formal test/ranking evaluation was run.

## 5. Mask Diagnostics

Masks stayed usable. Alpha was biased open during the middle of the run, but did not fall into the hard-collapse entropy band.

Selected and final epoch-level mask health:

```text
epoch8 raw=0.7385/0.4106
epoch8 entropy=0.3464/0.6759
epoch8 budget=0.1692
epoch8 overlap=0.2971

epoch10 raw=0.6634/0.4166
epoch10 entropy=0.4689/0.6782
epoch10 budget=0.1134
epoch10 overlap=0.2714
```

## 6. Test Commands

AUC/AUC-PR:

```bash
CUDA_VISIBLE_DEVICES=1 python -u test_auc.py -d nell_v1 -e diag_v5_nell_v1_original_score_w05_allmodes_10ep \
  --gpu 0 \
  --score_mode original
```

Ranking:

```bash
CUDA_VISIBLE_DEVICES=1 python -u test_ranking.py -d nell_v1 -e diag_v5_nell_v1_original_score_w05_allmodes_10ep \
  --score_mode original
```

## 7. Test Results

```text
AUC 0.8943
AUC-PR 0.9230
MRR 0.7247
Hits@1 0.6469
Hits@5 0.8212
Hits@10 0.8531
```

## 8. Baseline and Paper Comparison

Same-environment NELL_v1 baseline:

```text
AUC 0.8691
AUC-PR 0.7967
MRR 0.5246
Hits@1 0.4750
Hits@10 0.5700
```

GraIL paper NELL-995 v1 targets:

```text
AUC-PR 0.8605
Hits@10 0.5950
```

This run exceeded both references on the primary metrics:

```text
same-env AUC-PR: 0.9230 vs 0.7967
same-env Hits@10: 0.8531 vs 0.5700
paper AUC-PR: 0.9230 vs 0.8605
paper Hits@10: 0.8531 vs 0.5950
```

It also exceeded the same-environment baseline on AUC, MRR, Hits@1, and Hits@5.

## 9. Conclusion

Positive formal NELL_v1 result.

This is the strongest v5 evidence so far. The current alpha-specific causal-only path does not beat WN18RR baselines, but it does produce a large NELL_v1 improvement under original scoring while preserving non-collapsed masks. The result is not an effect-loss result; effect loss remains disabled.

Do not tune this exact configuration using this test result. The next NELL_v1 work should be either a repeat/seed check or a comparison against the unmodified same-environment baseline protocol, not test-set-driven scalar tuning.

## 10. Next Step

Recommended next work:

```text
1. Run a repeat NELL_v1 validation/test only if needed to check stability, preferably with the same config and a clear pre-registered selection rule.
2. Consider FB237_v1 original-score/all-mode validation next, because NELL_v1 is now positive and WN18RR variants are negative.
3. Preserve the current conclusion: v5 mitigates mask collapse, is positive on NELL_v1, and is still negative on WN18RR_v1/v2/v4.
```
