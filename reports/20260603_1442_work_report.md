# 2026-06-03 14:42 Work Report

## Scope

Added a default-off training-time effect score clamp to address the effect score/loss explosion seen in `diag_v5_wn18rr_v2_detach_shortcut_15ep`.

## Code Change

Added:

```text
--effect_score_clamp
```

Default is `0.0`, which disables clamping and preserves existing behavior.

The clamp is applied only to `effect_pos` and `effect_neg` used for training-time `effect_loss`. It does not change:

```text
model forward outputs
score_mode definitions
test_auc.py
test_ranking.py
baseline path
```

## Verification

Syntax check:

```bash
python -m py_compile train.py managers/trainer.py
```

Smoke command:

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d WN18RR_v2 -e smoke_v5_effect_clamp \
  --use_causal_training \
  --num_epochs 1 \
  --batch_size 4 \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.1 \
  --effect_loss_warmup_epochs 0 \
  --effect_loss_ramp_epochs 5 \
  --effect_gradient_mode detach_shortcut \
  --effect_score_clamp 100 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect
```

Smoke result:

```text
best validation AUC 0.9264
best validation AUC-PR 0.9213
epoch1 effect_loss_weight=0.0200
epoch1 effect_loss_ramp_factor=0.2000
epoch1 effect_score_clamp=100.0
epoch1 raw causal=0.5979
epoch1 raw shortcut=0.3920
epoch1 entropy causal=0.0809
epoch1 entropy shortcut=0.6643
epoch1 budget_loss=0.2435
epoch1 overlap_loss=0.2141
```

## Conclusion

The parser and training path work. This is not a formal result.

## Next Action

Run a 15-epoch WN18RR_v2 validation/mask diagnostic with:

```text
effect_loss_warmup_epochs=10
effect_loss_ramp_epochs=10
effect_gradient_mode=detach_shortcut
effect_score_clamp=100
```

Do not run test evaluation unless mask health and score stability improve materially.
