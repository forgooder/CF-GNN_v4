# 2026-06-04 13:20 CST Work Report

## 1. Git commit / branch

```text
Branch: v5-alpha-beta-mask
Code commit before run: 21728c7
```

## 2. Goal

Run a validation-only stability repeat for `nell_v1` using the same current v5 mainline configuration that produced the formal positive NELL_v1 test. Do not run test again.

## 3. Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d nell_v1 -e repeat_v5_nell_v1_original_score_w05_allmodes_10ep \
  --gpu 0 \
  --use_causal_training \
  --num_epochs 10 \
  --batch_size 16 \
  --causal_loss_weight 0.5 \
  --effect_loss_weight 0.0 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_entropy_floor_weight 0.1 \
  --causal_mask_logit_l2_weight 0.002 \
  --mask_entropy_floor 0.2 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode original \
  --selection_metric auc_pr \
  --log_all_score_modes_validation
```

## 4. Validation

Best validation:

```text
original AUC/AUC-PR 0.9009/0.9175
```

All-mode validation snapshots:

```text
epoch1:
original 0.8832/0.9041
causal 0.8839/0.9031
shortcut 0.8878/0.9053
effect 0.4699/0.6224
causal_plus_effect 0.8773/0.9020

epoch3:
original 0.8990/0.9134
causal 0.9020/0.9135
shortcut 0.9059/0.9144
effect 0.4255/0.5875
causal_plus_effect 0.8997/0.9126

best point:
original 0.9009/0.9175
causal 0.9087/0.9206
shortcut 0.9049/0.9197
effect 0.3954/0.5430
causal_plus_effect 0.9081/0.9199

late point:
original 0.9091/0.9147
causal 0.9099/0.9166
shortcut 0.9160/0.9188
effect 0.4305/0.5831
causal_plus_effect 0.9058/0.9149
```

## 5. Test

No test was run. This was intentionally validation-only to avoid tuning from the prior NELL_v1 formal test.

## 6. Mask diagnostics

```text
epoch1 raw=0.2211/0.4482 entropy=0.3239/0.6861 budget=0.1226 overlap=0.0948
epoch3 raw=0.6007/0.4203 entropy=0.3058/0.6784 budget=0.1679 overlap=0.2378
epoch6 raw=0.6623/0.4156 entropy=0.2954/0.6772 budget=0.1794 overlap=0.2618
epoch9 raw=0.7081/0.4131 entropy=0.2862/0.6763 budget=0.1865 overlap=0.2835
epoch10 raw=0.6923/0.4145 entropy=0.3321/0.6770 budget=0.1677 overlap=0.2725
```

Mask health is acceptable but not ideal. Shortcut remains stable and non-collapsed. Causal raw is moderately high late and causal entropy is lower than FB237_v1, but it remains far from the v4 near-zero entropy/full-collapse pattern.

## 7. Same-environment baseline comparison

```text
Same-env NELL_v1 baseline:
AUC 0.8691
AUC-PR 0.7967
MRR 0.5246
Hits@1 0.4750
Hits@10 0.5700
```

This validation repeat exceeds the same-env baseline AUC/AUC-PR at every validation point.

## 8. Paper table comparison

```text
Paper NELL-995 v1:
AUC-PR 0.8605
Hits@10 0.5950
```

This validation repeat exceeds the paper AUC-PR target at every validation point. Hits@10 was not evaluated in this repeat.

## 9. Analysis

This is a positive validation-only repeat, though weaker than the formal NELL_v1 run. Best original validation AUC-PR is 0.9175 versus the formal run's 0.9234, but the repeat remains well above same-env and paper targets throughout.

All score-mode diagnostics show causal, shortcut, and causal_plus_effect can slightly exceed original at some points, while effect alone remains weak. This is consistent with the current mainline: keep effect loss disabled, and use original score for formal selection unless a future objective provides a stable effect signal.

## 10. Next steps

1. Do not run another NELL_v1 test for this exact configuration.
2. Summarize current evidence: NELL_v1 and FB237_v1 are positive, WN18RR_v1/v2/v4 are negative despite healthier masks.
3. If continuing experiments, next useful work is either a structural WN18RR objective change or an additional non-test validation replicate on another target dataset, not test-set tuning.
