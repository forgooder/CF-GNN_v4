# Causal-GraIL Work Report

## 1. Time

2026-06-03 00:08 CST.

## 2. Git State

Directory:

```bash
/space/CIGRAL_v4/CF-GNN_v5
```

Branch:

```bash
v5-alpha-beta-mask
```

Commit:

```bash
83d1728 Implement Causal-GraIL v5 alpha beta masks
```

`git fetch origin` was run with elevated permissions because `.git/FETCH_HEAD` is mounted read-only inside the sandbox. It completed successfully after approval.

Dirty state before report commit:

```text
M .gitignore
?? reports/
```

`.gitignore` was updated to ignore required forbidden outputs: `*.pth`, `*.log`, `*.mdb`, `*.lock`, and `data/`.

## 3. Environment

Conda environment:

```text
cignn_dgl * /space/conda/envs/cignn_dgl
```

Python:

```bash
/space/conda/envs/cignn_dgl/bin/python
Python 3.7.12
```

GPU:

```text
NVIDIA-SMI 470.256.02, Driver 470.256.02, CUDA 11.4
GPU 0: about 707 MiB used by desktop processes
GPU 1: about 8 MiB used, available for smoke tests
```

Inside the restricted sandbox `nvidia-smi` could not access the driver, but the elevated check showed both GPUs are available. Smoke tests were run with `CUDA_VISIBLE_DEVICES=1`; the program reports `cuda:0` because visible devices are remapped.

v5 parameters were confirmed present in `python train.py --help`:

```text
--effect_loss_warmup_epochs
--mask_gamma
--mask_budget_weight
--mask_overlap_weight
--causal_mask_target
--shortcut_mask_target
--relation_budget_path
```

## 4. Task Goal

First v5 stage startup:

1. Save project context to `reports/PROJECT_CONTEXT.md`.
2. Initialize and maintain `reports/RESULTS_LEDGER.md`.
3. Confirm branch, commit, environment, GPU, and v5 parameters.
4. Run baseline smoke and causal smoke on WN18RR_v1.
5. Write the first work report.

## 5. Commands Run

Key commands:

```bash
mkdir -p reports
cat reports/PROJECT_CONTEXT.md
cat reports/RESULTS_LEDGER.md
pwd
git branch
git log --oneline -3
git fetch origin
conda info --envs
which python
python --version
nvidia-smi
python train.py --help
CUDA_VISIBLE_DEVICES=1 python train.py -d WN18RR_v1 -e smoke_v5_baseline --num_epochs 1 --batch_size 4
CUDA_VISIBLE_DEVICES=1 python train.py -d WN18RR_v1 -e smoke_v5_causal --use_causal_training --num_epochs 1 --batch_size 4 --causal_loss_weight 1.0 --effect_loss_weight 0.1 --effect_loss_warmup_epochs 5 --mask_gamma 0.7 --mask_budget_weight 0.01 --mask_overlap_weight 0.01 --causal_mask_target 0.5 --shortcut_mask_target 0.3 --score_mode causal
```

## 6. Results

`smoke_v5_baseline` completed successfully.

Validation:

```text
best validation AUC: 0.910501385739481
best validation AUC-PR: 0.9262877136114596
epoch 1 loss: 46861.3359375
training AUC: 0.9072272200791989
training AUC-PR: 0.9243964955149157
epoch time: 57.15697956085205 seconds
best checkpoint: experiments/smoke_v5_baseline/best_graph_classifier.pth
```

`smoke_v5_causal` completed successfully.

Validation:

```text
best validation AUC: 0.9193877551020408
best validation AUC-PR: 0.9301888863879259
epoch 1 loss: 28333.326171875
training AUC: 0.8628842835715336
training AUC-PR: 0.8920048560997512
epoch time: 132.70266342163086 seconds
best checkpoint: experiments/smoke_v5_causal/best_graph_classifier.pth
```

No test AUC/AUC-PR or ranking metrics were run in this report. These are smoke validation results only.

## 7. Causal Mask Diagnostics

`smoke_v5_causal` epoch 1:

```text
causal_mask_raw_mean: 0.2165562229181723
shortcut_mask_raw_mean: 0.2540688654022389
causal_mask_mean: 0.4517665796824411
shortcut_mask_mean: 0.4784859047936935
causal_mask_entropy: 0.3069921295183388
shortcut_mask_entropy: 0.544928096548681
mask_budget_loss: 0.1545259400075305
mask_overlap_loss: 0.04212219237470337
effect_loss_weight: 0.0
```

Smoke-level mask health is good: raw masks are not near the v4 collapse extremes, entropy is non-zero, and effect loss warmup is active.

Existing pre-report log `causal_v5_wn18rr_v1_original_select.log` was found in the workspace. It appears to contain a completed 100 epoch run from before this session. Its late epochs show shortcut raw mask collapse risk:

```text
epoch 100 causal_mask_raw_mean: 0.8939359119508119
epoch 100 shortcut_mask_raw_mean: 0.008373121591569659
epoch 100 causal_mask_entropy: 0.01882671483875083
epoch 100 shortcut_mask_entropy: 0.014643298040996249
```

Interpretation: v5 starts healthier than v4 in smoke, but the discovered long run suggests the current first-round hyperparameters may still let the beta/shortcut branch collapse late in training. This should be verified with controlled formal runs and test evaluation.

## 8. Errors / Warnings

Warnings and environment issues:

```text
Matplotlib used /tmp for cache because /home/zz/.config/matplotlib is not writable.
Sandboxed nvidia-smi failed to access the NVIDIA driver.
Sandboxed git fetch failed because .git/FETCH_HEAD is read-only.
```

Both were resolved or worked around using approved elevated commands where needed.

No smoke training errors:

```text
No unrecognized v5 parameters.
No DGL shape errors.
No CUDA OOM.
No NaN observed.
```

Workspace note:

```text
causal_v5_wn18rr_v1_original_select.log
experiments/
data/WN18RR_v1/relation2id.json
```

These outputs existed or were generated but are now covered by `.gitignore` rules and were not staged for commit.

## 9. Interpretation

v5 branch and parameter surface are correct. Baseline path still runs with causal training disabled, so v5 has not broken the default GRAIL path at smoke level.

The causal smoke confirms alpha/beta mask code runs and logs the expected diagnostics. At epoch 1, masks are substantially healthier than the v4 collapse pattern.

However, the existing 100 epoch log shows late shortcut raw mask values approaching zero and entropy approaching zero. That means the first formal WN18RR_v1 configuration should not be treated as explanation-stable until full test metrics and mask diagnostics are reported.

## 10. Next Actions

Recommended next step:

```bash
CUDA_VISIBLE_DEVICES=1 nohup python -u train.py -d WN18RR_v1 -e causal_v5_wn18rr_v1_causal_select \
  --use_causal_training \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.1 \
  --effect_loss_warmup_epochs 5 \
  --mask_gamma 0.7 \
  --mask_budget_weight 0.01 \
  --mask_overlap_weight 0.01 \
  --causal_mask_target 0.5 \
  --shortcut_mask_target 0.3 \
  --score_mode causal \
  > causal_v5_wn18rr_v1_causal_select.log 2>&1 &
```

Before launching another long run, inspect whether `causal_v5_wn18rr_v1_original_select` already has a valid checkpoint and run the planned AUC/ranking tests if appropriate. Because the discovered original-select run shows shortcut collapse, consider a small ablation with stronger shortcut budget or lower gamma after collecting its test metrics.
