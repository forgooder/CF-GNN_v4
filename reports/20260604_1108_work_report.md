# 2026-06-04 11:08 CST Work Report

## 1. Git State

Branch: `v5-alpha-beta-mask`

Current code base:

```text
08b3c9c Add WN18RR v2 original score diagnostic report
923d0bc Add WN18RR v1 original score formal test report
636d312 Add all score mode validation diagnostics
```

No code changes were made in this round.

## 2. Goal

Run an analogous WN18RR_v4 validation-only diagnostic with original-score selection and all-score-mode validation logging, using the stable causal-only alpha-specific regularization path.

## 3. Training / Validation Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d WN18RR_v4 -e diag_v5_wn18rr_v4_original_score_w05_allmodes_5ep \
  --gpu 0 \
  --use_causal_training \
  --num_epochs 5 \
  --batch_size 16 \
  --causal_loss_weight 0.5 \
  --effect_loss_weight 0.0 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_entropy_floor_weight 0.1 \
  --causal_mask_logit_l2_weight 0.002 \
  --mask_entropy_floor 0.2 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode original \
  --selection_metric auc_pr \
  --log_all_score_modes_validation
```

## 4. Validation Result

Best validation checkpoint by original AUC-PR:

```text
original AUC/AUC-PR 0.9064/0.9143
```

All-mode snapshot at the best original checkpoint:

```text
original AUC/AUC-PR 0.9064/0.9143
causal AUC/AUC-PR 0.9063/0.9142
shortcut AUC/AUC-PR 0.9048/0.9133
effect AUC/AUC-PR 0.8225/0.8543
causal_plus_effect AUC/AUC-PR 0.9064/0.9143
```

Later validation checks were lower, including original AUC-PR around 0.8812-0.9134. The run did not approach the same-environment WN18RR_v4 baseline AUC-PR 0.9354.

## 5. Test Result

No test or ranking evaluation was run. Validation was clearly below baseline, so test-set access was not justified.

## 6. Mask Diagnostics

Masks were non-collapsed throughout the 5-epoch run.

Selected and final epoch-level mask health:

```text
epoch1 raw=0.4346/0.4320
epoch1 entropy=0.3800/0.6814
epoch1 budget=0.1264
epoch1 overlap=0.1785

epoch5 raw=0.5460/0.4256
epoch5 entropy=0.4624/0.6811
epoch5 budget=0.1058
epoch5 overlap=0.2263
```

This is a clear improvement over the earlier WN18RR_v4 no-logit-L2 / effect-loss collapse modes, but the scorer remains weak.

## 7. Baseline and Paper Comparison

Same-environment WN18RR_v4 baseline:

```text
AUC 0.9374
AUC-PR 0.9354
MRR 0.7645
Hits@10 0.7635
```

The validation AUC-PR 0.9143 did not exceed the same-environment baseline. Hits@10 was not evaluated because the run did not qualify for test/ranking.

## 8. Conclusion

Negative validation result.

WN18RR_v4 repeats the current WN18RR_v1/v2 pattern: alpha-specific causal-only regularization keeps masks usable, but the validation score is below the same-environment GRAIL baseline. The all-mode snapshot shows no alternative score mode that rescues performance.

This strengthens the conclusion that v5's present objective mostly solves mask collapse diagnostics without producing stable AUC-PR/Hits@10 gains.

## 9. Next Step

Do not test this configuration.

Recommended next work:

```text
1. Stop broad WN18RR tuning for this v5 objective unless a structural objective change is introduced.
2. Run one NELL_v1 original-score/all-mode validation diagnostic, because prior NELL_v1 alpha-specific validation was the most promising non-WN18RR signal.
3. If NELL_v1 also fails under original-score/all-mode selection, write a v6 plan centered on preserving the baseline scorer while using masks as diagnostics or auxiliary constraints.
```
