# 2026-06-04 11:01 CST Work Report

## 1. Git State

Branch: `v5-alpha-beta-mask`

Current code base:

```text
923d0bc Add WN18RR v1 original score formal test report
636d312 Add all score mode validation diagnostics
f90fb26 Add configurable validation selection metric
```

No code changes were made in this round.

## 2. Goal

Run a WN18RR_v2 validation-only diagnostic using the currently most stable v5 path: causal-only training, alpha-specific regularization, original score selection by AUC-PR, and all-score-mode validation logging.

## 3. Training / Validation Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d WN18RR_v2 -e diag_v5_wn18rr_v2_original_score_w05_allmodes_5ep \
  --gpu 0 \
  --use_causal_training \
  --num_epochs 5 \
  --batch_size 16 \
  --causal_loss_weight 0.5 \
  --effect_loss_weight 0.0 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_entropy_floor_weight 0.1 \
  --causal_mask_logit_l2_weight 0.002 \
  --mask_entropy_floor 0.2 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode original \
  --selection_metric auc_pr \
  --log_all_score_modes_validation
```

## 4. Validation Result

Best validation checkpoint by original AUC-PR:

```text
original AUC/AUC-PR 0.9281/0.9272
```

All-mode snapshot at the selected validation point:

```text
original AUC/AUC-PR 0.9281/0.9272
causal AUC/AUC-PR 0.9280/0.9270
shortcut AUC/AUC-PR 0.9275/0.9261
effect AUC/AUC-PR 0.6170/0.7565
causal_plus_effect AUC/AUC-PR 0.8384/0.8919
```

The run did not meet the threshold for test evaluation. Best validation AUC-PR stayed far below the same-environment WN18RR_v2 baseline AUC-PR 0.9479 and below the GraIL paper WN18RR_v2 AUC-PR 0.9418.

## 5. Test Result

No test or ranking evaluation was run. Validation was clearly below baseline, so test-set access was not justified.

## 6. Mask Diagnostics

Mask health improved versus the older WN18RR_v2 effect-loss runs. There was no shortcut-zero collapse and no entropy collapse.

Selected/final epoch-level mask health:

```text
epoch4 raw=0.6459/0.4180
epoch4 entropy=0.5402/0.6790
epoch4 budget=0.0838
epoch4 overlap=0.2667

epoch5 raw=0.6088/0.4211
epoch5 entropy=0.5596/0.6801
epoch5 budget=0.0728
epoch5 overlap=0.2529
```

The main warning is that the causal raw mask stayed moderately high relative to the 0.45 target, but it did not approach the v4-style full-open collapse.

## 7. Baseline and Paper Comparison

Same-environment WN18RR_v2 baseline:

```text
AUC 0.9459
AUC-PR 0.9479
MRR 0.8024
Hits@10 0.8118
```

Paper WN18RR_v2 targets:

```text
AUC-PR 0.9418
Hits@10 0.7868
```

This validation-only run did not exceed either AUC-PR reference, and it did not qualify for Hits@10 testing.

## 8. Conclusion

Negative validation result.

The alpha-specific causal-only path fixes the earlier WN18RR_v2 failure mode where effect loss drove shortcut collapse or scorer explosion. However, the validation ceiling remains too low: best original AUC-PR was 0.9272 versus the same-environment baseline 0.9479. All score modes were similar or weaker, and `causal_plus_effect` was substantially weaker than `original`.

This supports the current diagnosis: v5 can produce healthier masks, but the present objective does not create a stronger scorer on WN18RR_v2.

## 9. Next Step

Do not test this configuration.

Recommended next work:

```text
1. Treat WN18RR_v2 original-score alpha-specific causal-only as negative.
2. Run one analogous all-mode validation diagnostic on WN18RR_v4 only if needed for cross-dataset confirmation.
3. If WN18RR_v4 repeats the same pattern, stop broad WN18RR tuning and write a v6 objective plan focused on using masks without suppressing the baseline scorer.
```
