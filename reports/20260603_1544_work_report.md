# 2026-06-03 15:44 Work Report

## Scope

Ran a WN18RR_v4 5-epoch validation/mask diagnostic using the stronger-beta-budget configuration after the WN18RR_v4 smoke showed low causal mask entropy.

## Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d WN18RR_v4 -e diag_v5_wn18rr_v4_stronger_beta_5ep \
  --use_causal_training \
  --num_epochs 5 \
  --batch_size 16 \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.1 \
  --effect_loss_warmup_epochs 10 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect
```

No test-set evaluation was run.

## Validation

```text
best validation AUC 0.9120
best validation AUC-PR 0.9139
```

## Mask Diagnostics

```text
epoch1 raw causal=0.4626
epoch1 raw shortcut=0.4093
epoch1 entropy causal=0.2114
epoch1 entropy shortcut=0.6711

epoch2 raw causal=0.0354
epoch2 raw shortcut=0.4479
epoch2 entropy causal=0.0329
epoch2 entropy shortcut=0.6871

epoch4 raw causal=0.9744
epoch4 raw shortcut=0.3545
epoch4 entropy causal=0.0306
epoch4 entropy shortcut=0.6491

epoch5 raw causal=0.9766
epoch5 raw shortcut=0.3551
epoch5 entropy causal=0.0246
epoch5 entropy shortcut=0.6494
```

## Conclusion

Negative diagnostic. WN18RR_v4 repeats the alpha-collapse pattern before effect loss turns on. Shortcut remains comparatively healthy, but causal mask entropy is low from epoch2 onward and causal raw saturates near one by epochs4-5.

This configuration should not be promoted to test evaluation.

## Next Action

The next code-level change should target alpha stability directly, for example staged mask regularization or a stronger entropy/budget mechanism active before effect loss. Continuing dataset-by-dataset formal runs with the current stronger-beta-budget setup is unlikely to answer the project goal positively.
