# 2026-06-03 18:57 CST Work Report

## Scope

Ran a 10 epoch validation-only `nell_v1` diagnostic with the new alpha-specific regularizers. No test-set evaluation was run.

## Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d nell_v1 -e diag_v5_nell_v1_alpha_specific_regs_10ep \
  --use_causal_training \
  --num_epochs 10 \
  --batch_size 16 \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.0 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_entropy_floor_weight 0.1 \
  --causal_mask_logit_l2_weight 0.002 \
  --mask_entropy_floor 0.2 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect
```

## Result

Best validation AUC was 0.9191 at epoch6, with AUC-PR 0.9206. Best observed validation AUC-PR was 0.9244 at epoch10.

Key mask checkpoints:

```text
epoch1 raw=0.4027/0.4352 entropy=0.4849/0.6830 causal_logit_l2=4.0239
epoch6 raw=0.7194/0.4118 entropy=0.3316/0.6761 mask_logit_l2=11.1772 causal_logit_l2=11.0379
epoch10 raw=0.6754/0.4157 entropy=0.4227/0.6778 budget=0.1330 overlap=0.2749 causal_logit_l2=7.4246
```

## Conclusion

This is a positive validation/mask diagnostic. Alpha-specific regularization keeps causal entropy well above the previous 0.1-0.12 failure band through epoch10 while beta remains healthy. Validation AUC-PR also improves over the prior NELL_v1 10 epoch causal-only diagnostic.

## Next

Do not run test evaluation yet. The next step should be a longer validation-only run or a repeated validation protocol before spending test-set budget.
