# 2026-06-03 12:36 Work Report

## Scope

Completed and evaluated the WN18RR_v2 formal stronger-beta-budget run after the successful WN18RR_v2 smoke.

## Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d WN18RR_v2 -e causal_v5_wn18rr_v2_stronger_beta_budget \
  --use_causal_training \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.1 \
  --effect_loss_warmup_epochs 10 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect
```

## Execution

The run was configured for 100 epochs with batch size 16. It was intentionally stopped after epoch 12 because the mask diagnostics showed persistent collapse immediately after the effect loss warmup ended.

Best validation checkpoint before early stop:

```text
best validation AUC 0.9319
validation AUC-PR at best-AUC checkpoint 0.9259
best observed validation AUC-PR around epoch 5: 0.9286
```

## Mask Diagnostics

Before effect loss became active:

```text
epoch5 raw causal=0.9150
epoch5 raw shortcut=0.3619
epoch5 entropy causal=0.0666
epoch9 raw causal=0.9670
epoch9 raw shortcut=0.3556
epoch9 entropy causal=0.0086
```

After effect loss became active:

```text
epoch11 effect_loss_weight=0.1
epoch11 raw causal=0.6727
epoch11 raw shortcut=0.0007
epoch11 entropy causal=0.0052
epoch11 entropy shortcut=0.0017
epoch11 mask_budget_loss=0.4703
epoch12 raw causal=0.7004
epoch12 raw shortcut=0.00003
epoch12 entropy causal=0.0034
epoch12 entropy shortcut=0.00027
epoch12 mask_budget_loss=0.4740
```

This is a failed mask-health result. Alpha entropy collapses before effect loss turns on, and beta collapses almost immediately after `effect_loss_weight` becomes non-zero.

## Test AUC

Tested the best validation-AUC checkpoint across score modes:

| Score Mode | AUC | AUC-PR |
|---|---:|---:|
| original | 0.9263 | 0.9268 |
| causal | 0.9235 | 0.9254 |
| effect | 0.8204 | 0.8613 |
| causal_plus_effect | 0.9275 | 0.9264 |

Best AUC-PR mode was `original`.

## Ranking

Ranking with `original`:

```text
MRR 0.7391
Hits@1 0.7136
Hits@5 0.7602
Hits@10 0.7628
```

## Conclusion

Negative formal early-stop result. The stronger-beta-budget setup does not solve WN18RR_v2 mask collapse. It also remains below the WN18RR_v2 paper targets:

```text
AUC-PR target 0.9418
Hits@10 target 0.7868
```

The likely failure mode is not only insufficient beta budget. Alpha has already become nearly binary before the effect branch is trained, and the full `effect_loss_weight=0.1` then drives shortcut mask collapse.

## Next Action

Run a WN18RR_v2 diagnostic focused on effect-loss onset, using a smaller effect loss weight or slower effect schedule. Judge by validation and mask health before spending full runs on WN18RR_v4.
