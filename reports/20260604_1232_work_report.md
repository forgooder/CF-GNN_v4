# 2026-06-04 12:32 CST Work Report

## 1. Git commit / branch

```text
Branch: v5-alpha-beta-mask
Code commit before report: cfad674
```

## 2. Goal

Validate whether the current v5 mainline, causal-only training with original-score selection, alpha-specific regularization, and all score-mode diagnostics, also transfers to `fb237_v1`.

## 3. Commands

The first command used the uppercase dataset name and failed because the local dataset directory is lowercase:

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d FB237_v1 -e diag_v5_fb237_v1_original_score_w05_allmodes_10ep \
  --gpu 0 --use_causal_training --num_epochs 10 --batch_size 16 \
  --causal_loss_weight 0.5 --effect_loss_weight 0.0 \
  --mask_gamma 0.5 --mask_budget_weight 0.05 --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_entropy_floor_weight 0.1 --causal_mask_logit_l2_weight 0.002 \
  --mask_entropy_floor 0.2 --causal_mask_target 0.45 --shortcut_mask_target 0.45 \
  --score_mode original --selection_metric auc_pr --log_all_score_modes_validation
```

Failure:

```text
FileNotFoundError: data/FB237_v1/train.txt
```

Rerun with the local dataset name:

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d fb237_v1 -e diag_v5_fb237_v1_original_score_w05_allmodes_10ep_lc \
  --gpu 0 --use_causal_training --num_epochs 10 --batch_size 16 \
  --causal_loss_weight 0.5 --effect_loss_weight 0.0 \
  --mask_gamma 0.5 --mask_budget_weight 0.05 --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_entropy_floor_weight 0.1 --causal_mask_logit_l2_weight 0.002 \
  --mask_entropy_floor 0.2 --causal_mask_target 0.45 --shortcut_mask_target 0.45 \
  --score_mode original --selection_metric auc_pr --log_all_score_modes_validation
```

Formal test, run once after validation and mask health qualified:

```bash
CUDA_VISIBLE_DEVICES=1 python -u test_auc.py -d fb237_v1 -e diag_v5_fb237_v1_original_score_w05_allmodes_10ep_lc --gpu 0 --score_mode original
CUDA_VISIBLE_DEVICES=1 python -u test_ranking.py -d fb237_v1 -e diag_v5_fb237_v1_original_score_w05_allmodes_10ep_lc --score_mode original
```

## 4. Validation

Best validation:

```text
original AUC/AUC-PR 0.8443/0.8632
```

Validation all-score snapshots:

```text
early saved point:
original 0.8310/0.8564
causal 0.8275/0.8591
shortcut 0.8245/0.8543
effect 0.6570/0.7396
causal_plus_effect 0.8225/0.8554

best saved point:
original 0.8443/0.8632
causal 0.8410/0.8615
shortcut 0.8417/0.8638
effect 0.6777/0.7215
causal_plus_effect 0.8356/0.8531

late points:
original 0.8325/0.8440
original 0.8304/0.8439
```

## 5. Test

```text
AUC 0.8396
AUC-PR 0.8546
MRR 0.5575
Hits@1 0.4492
Hits@5 0.6829
Hits@10 0.7622
```

## 6. Mask diagnostics

```text
epoch1 raw=0.6553/0.4169 entropy=0.4238/0.6776 budget=0.1315 overlap=0.2736
epoch5 raw=0.5819/0.4221 entropy=0.3507/0.6792 budget=0.1501 overlap=0.2434
epoch6 raw=0.4560/0.4312 entropy=0.3345/0.6819 budget=0.1433 overlap=0.1953
epoch9 raw=0.6234/0.4200 entropy=0.4622/0.6793 budget=0.1117 overlap=0.2595
epoch10 raw=0.5376/0.4259 entropy=0.4054/0.6809 budget=0.1241 overlap=0.2259
```

Mask health is acceptable. The shortcut mask does not collapse toward zero, causal entropy stays well above the old near-zero failure band, and budget/overlap losses do not dominate total loss. The causal mask is still moderately open, but not saturated.

## 7. Same-environment baseline comparison

```text
Same-env FB237_v1 baseline:
AUC 0.7938
AUC-PR 0.8349
MRR 0.4862
Hits@1 0.4024
Hits@10 0.6537
```

This run exceeds the same-environment baseline on AUC, AUC-PR, MRR, Hits@1, and Hits@10.

## 8. Paper table comparison

```text
Paper FB15k-237 v1:
AUC-PR 0.8469
Hits@10 0.6415
```

This run exceeds the paper AUC-PR and Hits@10 targets.

## 9. Analysis

This is a positive formal result. The current v5 mainline now has formal wins on `nell_v1` and `fb237_v1`, while WN18RR_v1/v2/v4 remain negative. The FB237 validation curve is not monotonic, but several validation points are above the same-env baseline and the saved best point is not paired with mask collapse.

The all-mode validation result is informative: shortcut and causal scores are close to original, while effect remains weak. This supports keeping effect loss out of the current mainline and using the original scorer for formal selection when validation qualifies.

## 10. Next steps

1. Do not tune this FB237_v1 configuration from the test result.
2. Run a controlled stability repeat on `nell_v1` or `fb237_v1` with the same configuration, preferably changing only the experiment seed/path if the code exposes a seed.
3. Deprioritize additional WN18RR scalar tuning until there is a structural objective change, because current runs fix mask collapse but not scorer quality there.
