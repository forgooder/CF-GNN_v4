# 2026-06-03 19:26 CST Work Report

## Scope

Ran a longer 20 epoch validation-only `nell_v1` diagnostic with alpha-specific regularization. No test-set evaluation was run.

## Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d nell_v1 -e diag_v5_nell_v1_alpha_specific_regs_20ep \
  --use_causal_training \
  --num_epochs 20 \
  --batch_size 16 \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.0 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_entropy_floor_weight 0.1 \
  --causal_mask_logit_l2_weight 0.002 \
  --mask_entropy_floor 0.2 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect
```

## Result

Best validation AUC was 0.9127 at epoch8, with AUC-PR 0.9213. Best observed validation AUC-PR was 0.9219 at epoch10.

Key mask checkpoints:

```text
epoch2 raw=0.5249/0.4256 entropy=0.1847/0.6795
epoch8 raw=0.5189/0.4257 entropy=0.2665/0.6796
epoch10 raw=0.6820/0.4147 entropy=0.2322/0.6763
epoch15 raw=0.5516/0.4240 entropy=0.1982/0.6793
epoch20 raw=0.5634/0.4235 entropy=0.2657/0.6790 budget=0.1758 overlap=0.2274
```

## Conclusion

This is a mixed replication. Alpha-specific regularization continues to prevent the severe alpha collapse seen in earlier NELL_v1 diagnostics, and beta remains healthy. The run is weaker than the previous 10 epoch diagnostic, both in validation AUC-PR and in alpha entropy stability.

## Next

Do not run test evaluation. Next work should either repeat the promising configuration under a controlled protocol or run a small alpha-specific weight sweep before spending test-set budget.
