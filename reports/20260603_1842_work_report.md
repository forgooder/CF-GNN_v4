# 2026-06-03 18:42 CST Work Report

## Scope

Added and smoke-tested default-off alpha-specific mask regularizers.

## Code Change

New flags:

```text
--causal_mask_entropy_floor_weight
--causal_mask_logit_l2_weight
```

The flags add extra causal-alpha-only entropy-floor and logit-L2 terms when explicitly enabled. Both default to 0.0.

## Smoke Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d nell_v1 -e smoke_v5_nell_v1_alpha_specific_regs \
  --use_causal_training \
  --num_epochs 1 \
  --batch_size 4 \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.0 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_entropy_floor_weight 0.1 \
  --causal_mask_logit_l2_weight 0.002 \
  --mask_entropy_floor 0.2 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect
```

## Result

Best validation AUC/AUC-PR was 0.8878/0.8997.

Epoch 1 diagnostics:

```text
raw=0.5751/0.4222
entropy=0.3430/0.6780
mask_reg_loss=0.0518
causal_mask_entropy_floor_loss=0.00138
causal_mask_logit_l2_loss=13.8667
```

## Conclusion

The code smoke passed. The new parser flags are accepted, training completes, and the new alpha-specific diagnostics are logged. This is not a performance result.

## Next

Run a longer validation-only NELL_v1 diagnostic with alpha-specific regularization to see whether alpha entropy stays above the prior failure band.
