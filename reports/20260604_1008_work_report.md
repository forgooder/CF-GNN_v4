# 2026-06-04 10:08 CST Work Report

## 1. Git State

Branch: `v5-alpha-beta-mask`

Current code base:

```text
849084b Add WN18RR v1 tiny effect diagnostic report
6061c11 Add WN18RR v1 causal-only diagnostics report
f90fb26 Add configurable validation selection metric
```

No code changes were made in this round.

## 2. Goal

Check whether WN18RR_v1 causal-only validation is being hurt by `causal_plus_effect` score composition. This run evaluates and selects by the direct `causal` score instead.

## 3. Command

```bash
CUDA_VISIBLE_DEVICES=1 python -u train.py -d WN18RR_v1 -e diag_v5_wn18rr_v1_alpha_specific_causal_score_aucpr_8ep \
  --gpu 0 \
  --use_causal_training \
  --num_epochs 8 \
  --batch_size 16 \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.0 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --mask_logit_l2_weight 0.001 \
  --causal_mask_entropy_floor_weight 0.1 \
  --causal_mask_logit_l2_weight 0.002 \
  --mask_entropy_floor 0.2 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal \
  --selection_metric auc_pr
```

## 4. Validation Result

Best validation result:

```text
AUC 0.9171
AUC-PR 0.9193
```

No test-set evaluation was run.

## 5. Mask Diagnostics

```text
epoch1 raw=0.3945/0.4379 entropy=0.3849/0.6827 budget=0.1203 overlap=0.1645
epoch3 raw=0.6303/0.4196 entropy=0.5820/0.6796 budget=0.0621 overlap=0.2622
epoch6 raw=0.8464/0.4023 entropy=0.3435/0.6732 budget=0.1849 overlap=0.3382
epoch7 raw=0.5848/0.4228 entropy=0.6465/0.6809 budget=0.0333 overlap=0.2461
epoch8 raw=0.6558/0.4175 entropy=0.5891/0.6791 budget=0.0653 overlap=0.2718
```

## 6. Baseline and Paper Comparison

Same-environment WN18RR_v1 baseline:

```text
AUC 0.9390
AUC-PR 0.9350
MRR 0.7976
Hits@10 0.8404
```

Paper WN18RR_v1 targets:

```text
AUC-PR 0.9432
Hits@10 0.8245
```

This run did not exceed same-environment baseline or paper AUC-PR. Hits@10 was not evaluated because validation failed.

## 7. Conclusion

Direct `causal` scoring is not better than the previous `causal_plus_effect` validation path. It peaks at AUC-PR 0.9193, while the best recent `causal_plus_effect` diagnostic reached 0.9266 and the same-environment baseline is 0.9350.

Mask health is acceptable, so the failure is not v4-style mask collapse. The current causal-only objective appears to produce healthy masks but a weaker WN18RR_v1 scoring function.

## 8. Next Step

Do not run test evaluation.

Next low-risk WN18RR_v1 diagnostic should reduce causal loss pressure rather than changing score mode:

```text
causal_loss_weight 0.3 to 0.5
effect_loss_weight 0.0
score_mode causal_plus_effect or original for validation comparison
selection_metric auc_pr
```
