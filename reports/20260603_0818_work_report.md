# Causal-GraIL Work Report

## 1. Time

2026-06-03 08:18 CST.

## 2. Git State

Directory:

```bash
/space/CIGRAL_v4/CF-GNN_v5
```

Branch:

```bash
v5-alpha-beta-mask
```

Starting commit for this session:

```bash
3de50f9 Add v5 startup smoke report
```

Target implementation commit remains present in history:

```bash
83d1728 Implement Causal-GraIL v5 alpha beta masks
```

## 3. Task Goal

Evaluate the existing completed 100 epoch WN18RR_v1 v5 run:

```text
causal_v5_wn18rr_v1_original_select
```

The goal was to convert the discovered long run from an unreported workspace artifact into a formal ledger result, including test AUC/AUC-PR, ranking metrics, mask diagnostics, and a conclusion against the same-environment GRAIL baseline.

## 4. Commands Run

Status and context:

```bash
git status --short --branch
git log --oneline -5
sed -n '1,220p' reports/PROJECT_CONTEXT.md
sed -n '1,240p' reports/RESULTS_LEDGER.md
sed -n '1,220p' experiments/causal_v5_wn18rr_v1_original_select/params.json
tail -40 causal_v5_wn18rr_v1_original_select.log
```

Test AUC/AUC-PR for all score modes:

```bash
for mode in original causal effect causal_plus_effect; do
  CUDA_VISIBLE_DEVICES=1 python test_auc.py -d WN18RR_v1 \
    -e causal_v5_wn18rr_v1_original_select \
    --score_mode "$mode" \
    --gpu 0 \
    --batch_size 16
done
```

Ranking for the best AUC-PR score mode:

```bash
CUDA_VISIBLE_DEVICES=1 python test_ranking.py -d WN18RR_v1 \
  -e causal_v5_wn18rr_v1_original_select \
  --score_mode causal_plus_effect \
  -m sample
```

## 5. Experiment Config

From `experiments/causal_v5_wn18rr_v1_original_select/params.json`:

```text
dataset: WN18RR_v1
num_epochs: 100
batch_size: 16
lr: 0.01
edge_dropout: 0.5
use_causal_training: true
causal_loss_weight: 1.0
effect_loss_weight: 0.1
effect_loss_warmup_epochs: 5
mask_gamma: 0.7
mask_budget_weight: 0.01
mask_overlap_weight: 0.01
causal_mask_target: 0.5
shortcut_mask_target: 0.3
score_mode during training/selection: original
```

## 6. Test AUC/AUC-PR

WN18RR_v1 test set:

```text
original:           AUC 0.9283, AUC-PR 0.9265
causal:             AUC 0.9250, AUC-PR 0.9222
effect:             AUC 0.9072, AUC-PR 0.9156
causal_plus_effect: AUC 0.9315, AUC-PR 0.9293
```

Best AUC-PR mode was `causal_plus_effect`.

## 7. Ranking

WN18RR_v1 sample ranking with `causal_plus_effect`:

```text
MRR 0.6989
Hits@1 0.6411
Hits@5 0.7641
Hits@10 0.7727
```

## 8. Causal Mask Diagnostics

Epoch 100:

```text
causal_mask_raw_mean: 0.8939359119508119
shortcut_mask_raw_mean: 0.008373121591569659
causal_mask_mean: 0.9283850411046571
shortcut_mask_mean: 0.30589366187739864
causal_mask_entropy: 0.01882671483875083
shortcut_mask_entropy: 0.014643298040996249
mask_budget_loss: 0.333784653022226
mask_overlap_loss: 0.00722880513777457
```

This violates the project mask health standard. The shortcut raw mask is near zero and both entropies are near zero, indicating late mask collapse.

## 9. Baseline Comparison

Recorded same-environment WN18RR_v1 GRAIL baseline from project context:

```text
AUC 0.9390
AUC-PR 0.9350
MRR 0.7976
Hits@10 0.8404
```

Best v5 score mode in this run:

```text
AUC 0.9315
AUC-PR 0.9293
MRR 0.6989
Hits@10 0.7727
```

The run underperforms baseline on AUC, AUC-PR, MRR, and Hits@10.

## 10. Conclusion

`causal_v5_wn18rr_v1_original_select` is a formal negative result.

It does not exceed same-environment GRAIL baseline, and it does not mitigate the v4-style collapse pattern over a full 100 epoch run. The current first-round v5 regularizers are too weak or poorly targeted for late shortcut/beta preservation.

## 11. Next Actions

Recommended next run should stay on WN18RR_v1 before expanding datasets:

```bash
CUDA_VISIBLE_DEVICES=1 nohup python -u train.py -d WN18RR_v1 -e causal_v5_wn18rr_v1_stronger_beta_budget \
  --use_causal_training \
  --causal_loss_weight 1.0 \
  --effect_loss_weight 0.1 \
  --effect_loss_warmup_epochs 10 \
  --mask_gamma 0.5 \
  --mask_budget_weight 0.05 \
  --mask_overlap_weight 0.01 \
  --causal_mask_target 0.45 \
  --shortcut_mask_target 0.45 \
  --score_mode causal_plus_effect \
  > causal_v5_wn18rr_v1_stronger_beta_budget.log 2>&1 &
```

Rationale: lower gamma reduces hard early saturation; equal causal/shortcut targets and stronger budget weight directly penalize the observed shortcut collapse. This is still within project constraints because it does not alter data, negative sampling, subgraph extraction, or metric calculation.
